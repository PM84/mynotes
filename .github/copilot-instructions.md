# Copilot Instructions

## Terminal-Ausgaben

- Terminalbefehle dürfen **nicht** gepiped werden (`| tail`, `| head`, `| grep`, `2>&1 | ...` etc.), damit die vollständige Ausgabe für den Entwickler im Terminal sichtbar bleibt.
