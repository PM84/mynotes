Du erhältst ein Bild aus einem digitalen Zeichenbereich (Skizze, Handschrift, Diagramm).

Aufgabe: {task}

Antworte ausschließlich auf Deutsch und ausschließlich in **GitHub-Flavored Markdown**, ohne einleitende Floskeln, ohne Code-Block-Wrapping um die gesamte Antwort. Verwende Überschriften, Listen, Tabellen oder LaTeX-Formeln (`$...$` / `$$...$$`) wo sinnvoll. Wenn etwas unleserlich ist, kennzeichne es mit `[unleserlich]`.
