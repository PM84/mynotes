# lamp_server_docker

## Add sourcecode of the website

Clone a repository with the sourcecode of the website to the root of this repository.

```
/www/{sourcecode}
```

Place a db dump that should be seeded to the db in the `/dump` directory.

```
/dump/{sqlfilename}.sql
```